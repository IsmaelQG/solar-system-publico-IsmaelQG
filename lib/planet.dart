import 'package:flutter/cupertino.dart';

class Planet{
  String image;
  String name;
  double distance;
  String data;
  Color color;

  Planet(this.color, this.image, this.name, this.distance, this.data);
}